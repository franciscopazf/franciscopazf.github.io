---
export interface Props {
  class?: string;
}
const { class: cls = '' } = Astro.props as Props;
---
<div class={`rounded-2xl bg-zinc-800/60 border border-white/6 p-6 shadow-lg backdrop-blur-md ${cls}`}>
  <slot />
</div>
